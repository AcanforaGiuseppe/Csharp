{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft": "Warning",
      "Microsoft.Hosting.Lifetime": "Information",
      // We're adding this row for showing the SQL queries in the output window, section Asp.net core web server
      "Microsoft.EntityFrameworkCore.Database.Command": "Information"
    }
  },
  "AllowedHosts": "*",
  // In the connection string, we write the connection to the database
  "ConnectionStrings": {
    //                    Server= "Link to server db"   ;Database="DatabaseName"; "To write for a trusted connection!! --->>>" Trusted_Connection=True;MultipleActiveResultSets=true
    "DefaultConnection": "Server=(LocalDb)\\MSSQLLocalDB;Database=Test_Database;Trusted_Connection=True;MultipleActiveResultSets=true"
  }
}